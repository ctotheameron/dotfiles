# AUR packages (installed with yay/paru if available).
# Lines starting with # are ignored.
asdf-vm
# Agent multiplexer, the Brewfile counterpart. herdr-bin tracks the same
# release as the Homebrew formula. The plain `herdr` AUR package lags behind.
herdr-bin
sesh-bin
opencode-bin
graphite-cli
1password-cli
# linear-tui (Linear TUI client) has no AUR package. Install on Arch with:
#   go install github.com/roeyazroel/linear-tui/cmd/linear-tui@latest
